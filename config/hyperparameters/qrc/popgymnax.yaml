# @package _global_

algorithm:
  trace_lambda: 0.95

q_optimizer:
  max_grad_norm: null
h_optimizer:
  max_grad_norm: null

total_timesteps: 5_000_000
num_epochs: 25

epsilon_start: 1.0
epsilon_end: 0.01
epsilon_fraction: 0.2
