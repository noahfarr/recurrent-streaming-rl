# @package _global_
defaults:
  - override /algorithm: ppo
  - override /mode: rtrl
  - override /cell: rtu
  - override /logger: [dashboard,wandb]

num_seeds: 1
seed: 0

cell:
  config:
    features: 64
    activation_fn:
      _target_: src.utils.identity.identity
      _partial_: true
    output_activation_fn:
      _target_: jax.numpy.tanh
      _partial_: true

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      seed: 0,1,2,3,4
      environment: popgymnax/easy/repeat_first,popgymnax/easy/count_recall,popgymnax/easy/concentration,popgymnax/easy/autoencode,popgymnax/easy/higher_lower
